# 个人微信视觉 AI 助手 (GUI Agent) 开发指南

**核心目的与破局思路**
本项目旨在从零开发一款完全脱离底层协议、100% 免疫微信封号风控的"纯视觉"个人微信 AI 助手，并为未来的开源发布奠定标准化的工程基础。我们彻底摒弃了高危的内存 Hook 与 API 逆向，转而采用"GUI 智能体"架构：让程序像真实人类一样，通过将窗口拉至前台、截屏"看"消息、利用 OCR"读"文字、结合本地知识库与大模型大脑进行"思考"，最终通过物理模拟键鼠完成"回复"。这不仅是一套极低成本、极高隐私的自动化客服底座，更是全栈开发向多模态 AI Agent 进阶的绝佳实践。

---

## 一、 核心架构与工作流设计

为了兼顾性能、成本和成功率，系统将采用**分层监听与读写分离**的设计。每次循环必须严格遵循以下流水线：

1. **窗口激活与前台锁定 (Focus & Foreground)**
    - **动作**：在进行任何视觉捕获前，通过操作系统 API 获取微信窗口句柄，强制将其从后台唤醒、取消最小化，并置于屏幕最顶层。
    - **意义**：彻底解决窗口被其他软件遮挡、截图出现黑屏或截到其他内容导致的 OCR 彻底瘫痪问题，保证视觉输入的 100% 纯净度。
2. **状态轮询与轻量级监听 (Visual Polling)**
    - **动作**：**弃用消耗极高且极易误判的全区域 SSIM 对比**。对激活后的微信左侧会话列表进行极小特定区域截图。将其转为 HSV 色彩空间，仅寻找和匹配“未读消息红点”的特定红色像素，或通过提取红点旁最新消息的缩略文本块进行比对。
    - **意义**：避免由于群聊时间戳更新、打字状态闪动等引起的无效触发，极大降低 CPU 运算负荷，实现真正的"零 Token 消耗"安静待机。
    - **首次校准机制**：由于不同用户的屏幕分辨率、系统缩放比例和微信窗口大小各不相同，程序**首次启动时**需引导用户完成一次**交互式区域标定**：
        1. 程序弹窗提示"请用鼠标框选微信**左侧会话列表**区域"，用户拖拽后记录坐标 `session_list_rect`；
        2. 程序弹窗提示"请用鼠标框选微信**右侧聊天内容**区域"，用户拖拽后记录坐标 `chat_content_rect`；
        3. 程序弹窗提示"请用鼠标点击微信**底部输入框**中心位置"，用户点击后记录坐标 `input_box_center`；
        4. 将以上坐标作为相对于微信窗口左上角的**偏移量**保存到 `config.yaml`，后续运行通过窗口句柄实时获取窗口位置并换算绝对坐标。
    - **系统 DPI 缩放适配（核心避坑）**：由于 Windows 系统普遍存在 125%、150% 显示缩放，`pyautogui` 模拟鼠标的逻辑坐标将与 `mss` / `OpenCV` 截屏抓取的物理像素坐标产生严重偏差。**程序启动时必须强制调用 Windows DPI 感知 API** (`ctypes.windll.shcore.SetProcessDpiAwareness(2)`)，确保所有物理坐标系完全统一。
    - **浅色模式依赖要求**：MVP 阶段，**强制要求微信客户端保持在「浅色模式」**运行。视觉找图高度依赖气泡颜色和红点像素，开启深色模式（Dark Mode）会导致所有硬编码图像阈值失效。
    - **分辨率自适应**：如果微信窗口大小发生变化（用户手动调整或切换显示器），程序应检测到窗口尺寸变动后提示用户重新校准，或按比例自动缩放已保存坐标。
3. **语义提取 (OCR Parsing)**
    - **动作**：一旦检测到新消息提示（如绿色气泡或红点），截取聊天记录区域，送入本地轻量级 OCR 引擎/图像识别大模型提取纯文本及对应屏幕物理坐标。
    - **消息归属判断规则（核心难点）**：OCR 返回的是一堆"文字块 + 坐标"，但程序必须知道**哪句话是对方说的、哪句话是自己说的**。微信聊天界面的视觉布局有严格规律：
        - **对方消息**：气泡/文本块的 X 坐标偏向聊天区域**左侧**（`x < chat_width * 40%`）
        - **自己的消息**：气泡/文本块的 X 坐标偏向聊天区域**右侧**（`x > chat_width * 60%`）
        - **系统时间戳/通知**：文本块位于聊天区域**水平居中**位置（`40% <= x <= 60%`），字体通常为灰色小号，应直接**过滤忽略**
        - **多模态异常处理与兜底策略（核心机制）**：当 OCR 提取出 `[语音]`、`[图片]`、`[视频]`、`[文件]` 等特定系统提示文本，或遇到小程序/红包等无法解析的卡片元素时，说明接收到了暂不支持的多模态消息。此时**触发专门的兜底降级响应**，不送入大模型，直接由 Agent 执行固定人设回复话术：**“抱歉，我现在的设备听不了语音/看不了图片，麻烦发下文字哦~”**，以避免话题阻断或无反应卡顿。
    - **消息去重与状态管理（禁止依赖 Y 坐标）**：OCR 每次截图都会识别出屏幕上**所有可见消息**，但程序只应回复新增的、未处理过的消息。由于用户可能滚动鼠标，甚至后续新消息涌入也会顶替屏幕布局，任何带绝对坐标的去重机制都会引发“无限重复回复”的致命灾难。正确设计如下：
        1. 废弃对绝对 Y 坐标的依赖。改为对每条识别到的消息，计算纯内容哈希值（`MD5(消息文本内容 + 发送方标识)`）。
        2. 为了应对用户连续发送两条完全一模一样的短文本（导致哈希无法区分），引入**滑动窗口序列缓存**机制：不仅对比单条消息特征，还要将“最近 N 条连续消息的内容列表”作为当前对话进度的指纹进行保存与对比。
        3. 维护一个本地内存的 `已处理消息序列集`，以此确保绝对且唯一的消息消费。
        5. 集合大小超过阈值（如 500 条）时，移除最早的条目，防止内存无限增长
4. **大脑决策 (LLM + RAG)**
    - **动作**：将提取到的文本，结合预设的系统人设（Persona）以及本地向量数据库检索到的专业知识，组装成 Prompt 发送给大语言模型。
5. **拟人化执行 (Human-like Action)**
    - **动作**：计算输入框的绝对坐标，通过贝塞尔曲线移动鼠标点击激活，加入随机思考延迟后，将 AI 回复通过剪贴板安全注入并发送。
    - **详见下方"第四章：深度拟人化引擎"**。

---

## 二、 推荐技术栈 (向开源与跨平台对齐)

构建这套系统，建议采用前后端解耦的模式，方便后期打包开源。

- **核心控制大脑 (后端)**：`Python 3.10+`
    - **窗口与键鼠控制**：`pygetwindow` (跨平台窗口管理) + `pyautogui` / `pynput` (模拟操作)。
    - **视觉与图像**：`OpenCV` (`cv2`) 负责高效找图和对比，`mss` 负责极低延迟的屏幕截图。
    - **文本识别**：`PaddleOCR` (百度开源，对中文极度友好，本地运行极快)。
- **知识库与大模型对接**：
    - **RAG 框架**：`LangChain` 配合 `ChromaDB` (轻量级本地向量库)。
    - **LLM 接口**：采用兼容 OpenAI 格式的封装。这样既可以无缝对接云端 API 跑通 MVP，也能随时切回你本地部署的开源大模型（比如在独立的推理节点上跑），把隐私彻底锁在本地。
- **可视化控制台 (前端/未来规划)**：
    - 后期如果想加个漂亮的悬浮控制台来展示"思考过程"和管理知识库，直接上 `React` + `TypeScript` 配合 `Electron` 封装，这是全栈最顺手的打法，打包给开源社区也极其友好。

---

## 三、 分阶段开发路径

为控制工程复杂度，项目开发严格分为两个阶段。**先跑通 MVP 闭环，再逐步加载进阶能力。**

### MVP 阶段（核心目标：端到端闭环跑通）

| 步骤 | 任务 | 交付物 |
|------|------|--------|
| M1 | 环境初始化：安装 Python 依赖，创建项目骨架文件和 `config.yaml` | `requirements.txt`、项目目录结构 |
| M2 | 窗口控制模块：实现微信窗口的发现、激活、前台锁定 | `window_manager.py` |
| M3 | 截屏与视觉差异检测：实现区域截屏、首次校准交互、SSIM 差异对比 | `vision.py` |
| M4 | OCR 提取与消息解析：集成 PaddleOCR，实现消息文本提取 + 归属判断 + 去重 | `ocr_parser.py` |
| M5 | LLM 直连（不上 RAG）：封装 OpenAI 兼容接口，设计基础人设 Prompt | `agent.py` |
| M6 | 执行层：实现剪贴板粘贴 + 物理键盘发送的基础回复链路 | `action.py` |
| M7 | 主循环串联：将 M2~M6 串入 `main.py`，完成一问一答闭环 | `main.py` |
| M8 | 基础拟人化：加入思考延迟（泊松分布）和作息时间墙 | `anti_risk.py` |

### 进阶阶段（核心目标：商业可用 + 开源就绪）

| 步骤 | 任务 | 交付物 |
|------|------|--------|
| A1 | RAG 知识库接入：集成 LangChain + ChromaDB，支持本地业务资料检索 | `rag_engine.py` |
| A2 | 深度拟人化：贝塞尔曲线鼠标轨迹、手误模拟、非匀速击键 | `anti_risk.py` 增强 |
| A3 | 多会话管理：支持自动切换左侧会话列表的不同联系人 | `session_manager.py` |
| A4 | Electron 可视化控制台：展示思考过程、管理知识库、一键启停 | `console/` 前端项目 |
| A5 | 开源打包：README、LICENSE、Docker 化部署、GitHub Actions CI | 项目根目录 |

---

## 四、 深度拟人化引擎 (Anti-Risk Engine)

纯粹的自动化极易被云端行为特征库识别。必须在代码逻辑中注入"人类的瑕疵"，让程序的每一个动作都带有随机性和生理特征。

### 4.1 泊松分布的"思考延迟" (Thinking Delay)
人类回复消息的耗时与消息长度呈正相关，且具备随机性。收到消息后，程序必须进入挂起状态。
- **公式参考**：`基础延迟 T = (接收消息字数 * 0.2秒) + random(1, 3)秒`
- 只有等待期结束，才允许向大模型发起请求。大模型返回后，再追加 `random(0.5, 1.5)秒` 的"打字前犹豫"。

### 4.2 仿生作息时间墙 (Biological Clock)
阻断 24 小时高频秒回的机器特征。
- **强制休眠期**：每日 `00:00 - 07:00` 不进行任何回复。
- **慢速时段**：午休 `12:00 - 13:30` 和晚餐 `18:00 - 19:30`，将整体回复延迟基数乘以 `1.5 ~ 2.0`，模拟"正在忙碌，偶尔看一眼电脑"的真实状态。
- **深夜降速**：`22:00 - 00:00` 回复延迟基数乘以 `1.3`，模拟困意渐浓。

### 4.3 非匀速与手误模拟 (Variable Keystrokes & Typos)
- 在使用物理模拟敲击的场景中，每次按键间隔必须在 `0.08秒 ~ 0.25秒` 之间随机波动。
- **高级伪装**：设定 `2%` 的全局手误率。偶尔故意输入错误的相邻字母，停顿 `0.3 ~ 0.8秒` 后，模拟敲击 `Backspace` 键删除，再输入正确字母。
- **注意**：在 MVP 阶段我们采用剪贴板粘贴方案（`Ctrl+V`），手误模拟为进阶阶段功能。

### 4.4 鼠标轨迹的贝塞尔曲线 (Mouse Trajectory)
- **禁止瞬移**：鼠标指针在屏幕上必须走贝塞尔曲线路径，加入弧度和接近目标时的减速（Ease-Out）效果。
- **微抖动**：到达目标坐标后，在点击前加入 `+/-2~5像素` 的微小偏移抖动，模拟人手的不精确性。
- **进阶阶段实现**：MVP 阶段可先用 `pyautogui.moveTo()` 的内置 `duration` 参数做简单过渡。

---

## 五、 开发要点与避坑指南

### 1. 窗口前台激活的"系统级玄学"

在 Windows 系统下，频繁抢夺焦点极易触发系统的"防骚扰机制"（窗口只在任务栏闪烁而不弹到前台）。

- **避坑对策**：需要调用 `ctypes.windll.user32` 底层接口，在调用 `SetForegroundWindow` 之前，模拟一次按下 `Alt` 键的动作，可以100% 绕过 Windows 的限制，强制拉起微信。

### 2. OCR 性能优化：不要"大水漫灌"

- **避坑对策**：绝对不要把 1080P 或 4K 的全屏截图直接扔给 OCR。必须先用 OpenCV 裁剪出只包含"最新一条聊天气泡"的极小矩形区域（可能只有 300x100 像素），再进行 OCR 识别。这能把识别时间从 1-2 秒压缩到 50 毫秒以内。

### 3. 垂直领域的知识挂载 (RAG)

如果要让这个助手具备商业价值，知识库是灵魂。

- **开发思路**：把你的业务资料存入向量数据库。比如针对电商场景，把实体商品（如水晶、首饰）的材质说明、白皮书，或者数字服务（如 AI 测算报告）的价目表切片存入。当客户询问"这个手链是什么材质"时，大模型会优先从本地检索事实，避免胡编乱造。

### 4. 开源架构设计 (Open-Source Readiness)

目标是开源，第一天就要写好抽象层：

- 配置文件抽离：使用 `.env` 或 `config.yaml` 管理屏幕分辨率、OCR 引擎路径、API Key 等，绝对不要硬编码。
- 模块化设计：将 `Vision` (看)、`Agent` (想)、`Action` (做) 拆分成独立的 Python 类，方便社区后续贡献新的视觉模型或对接不同的聊天软件。

### 5. 异常与容错处理 (Fault Tolerance)

视觉方案最脆弱的地方在于截屏和 OCR 随时可能失败，必须构建多层防护网：

- **OCR 置信度过滤**：PaddleOCR 返回的每条文本都附带置信度分数（0~1）。当置信度低于阈值（建议 `0.7`）时，该条文字应**丢弃或标记为不可信**，绝不允许直接送入 LLM 产生幻觉回复。
- **窗口状态守护**：每次主循环开始前，必须校验微信窗口是否仍然存在（句柄有效性检查）。如果微信被用户手动关闭或崩溃退出，程序应进入**优雅降级模式**——暂停主循环并每隔 30 秒轮询微信进程是否重新启动，而不是直接抛异常崩溃。
- **焦点窗口二次验证（防错发保护）**：在执行 `Ctrl+V` 粘贴和 `Enter` 发送之前的**最后一刻**，必须通过 `GetForegroundWindow()` 再次确认当前最顶层窗口的标题/句柄仍然是微信。如果焦点已被其他弹窗抢占，则**立即中止本次操作**。
- **物理输入侦听锁（竞态条件防御）**：如果 Agent 正在执行“激活窗口 -> 点击 -> 粘贴 -> 发送”动作组时，真人用户刚好移动了鼠标或打字，将导致毁灭性的执行脱位。必须通过 `pynput` 等库侦听全局鼠标与键盘事件，当发现过去 3~5 秒内存在真实的物理输入，Agent 应判定当前处于“人类接管状态”，临时挂起当前的自动恢复流，直到物理输入停止后再重新获取焦点工作。
- **剪贴板竞争保护**：在写入剪贴板前，先备份当前剪贴板内容；写入完毕并确认发送后，立刻恢复原始剪贴板内容。
- **全局异常兜底**：主循环包裹在 `try-except` 中，任何未预期的异常被捕获并写入日志，程序在等待冷却期后自动尝试恢复运行，非毁灭性错误绝不直接退出。

---

## 六、 V1.1 版本规划：联系人专属人设系统

### 6.1 功能背景与需求分析

**当前状态**：
- 系统使用统一的 AI 人设（`core/agent.py` 中的 `system_prompt`）
- 所有联系人收到相同风格的回复
- 无法针对不同场景和对象调整语气

**用户需求**：
- 对工作伙伴使用专业、礼貌的商务语气
- 对亲密朋友使用轻松、幽默的口语化表达
- 对家人使用温暖、体贴的关怀语言
- 对特定客户使用定制化的服务话术

**技术可行性**：
✅ 已具备联系人识别能力（`core/engine.py:131-139`）
✅ 已有完善的多模型配置架构（M12阶段）可复用
✅ UI框架已预留人设管理页面（`ui/index.html:1471-1477`）
✅ 配置系统和后端API模式成熟

### 6.2 架构设计方案

#### 6.2.1 配置文件扩展

在 `data/config.yaml` 中新增人设配置段：

```yaml
# ============================================
# 联系人专属人设配置 (V1.1 新增)
# ============================================
contacts_personas:
  # 默认人设（当没有特定配置时使用）
  default:
    name: "默认人设"
    description: "适用于所有未配置特定人设的联系人"
    system_prompt: |
      你是微信的主人本人。你正在通过自动化程序代替主人和他的朋友、家人、同事聊天。
      你必须严格遵守以下行为准则，确保不被发现是机器代聊：
      1. 语气要温暖、自然、口语化，像朋友之间轻松聊天
      2. 回复要简短精炼，一般控制在一两句话以内（10~20个字最佳）
      3. 表达要积极友善，适当配合语气词（嗯、哈哈、好嘞、行、好的呀）
      4. 只输出你想发送的纯文本内容，不要包裹任何多余的解释
    enabled: true

  # 工作伙伴人设示例
  "张三":
    name: "工作伙伴"
    description: "专业、简洁、礼貌的商务语气"
    system_prompt: |
      你正在回复重要的工作伙伴，请注意：
      1. 语气要专业、简洁、礼貌，体现职业素养
      2. 回复要直接切中要点，避免过多的闲聊
      3. 工作相关用语要准确，可以用"好的"、"收到"、"明白"等确认词
      4. 适当使用"您"表示尊重，但不要过于正式
      5. 只输出回复内容，不要有任何解释性文字
    enabled: true

  # 好朋友人设示例
  "李四":
    name: "好朋友"
    description: "轻松、幽默、可以开玩笑的亲密朋友语气"
    system_prompt: |
      你正在回复最好的朋友，请注意：
      1. 语气要超级轻松、随意，就像平时聊天一样
      2. 可以开玩笑、调侃、使用网络流行语
      3. 回复可以简短甚至用表情包式的回复（如"哈哈"、"2333"、"绝了"）
      4. 可以适当吐槽、自嘲，不用太客气
      5. 纯聊天模式，只输出你想说的话
    enabled: true

# 人设模板库（快速创建新联系人时参考）
persona_templates:
  professional:
    name: "商务专业"
    description: "适用于客户、领导、商务伙伴"
    system_prompt: "（详细的商务人设模板）"
  casual:
    name: "轻松随意"
    description: "适用于朋友、同学、熟人"
    system_prompt: "（详细的轻松人设模板）"
  warm:
    name: "温暖关怀"
    description: "适用于家人、伴侣、亲密关系"
    system_prompt: "（详细的温暖人设模板）"
  service:
    name: "客服服务"
    description: "适用于客户服务场景"
    system_prompt: "（详细的客服人设模板）"
```

#### 6.2.2 核心代码修改

**AgentBrain 类扩展** (`core/agent.py`):

```python
class AgentBrain:
    def __init__(self, config_path="data/config.yaml"):
        self.config = self._load_config(config_path)
        # ... 现有初始化代码 ...

        # 加载联系人专属人设配置
        self.personas = self.config.get("contacts_personas", {})
        self.default_persona = self.personas.get("default", {}).get("system_prompt", self.system_prompt)

    def _get_persona_for_contact(self, contact_name: str = None) -> str:
        """根据联系人名字获取对应的系统人设"""
        if not contact_name:
            return self.default_persona

        # 精确匹配
        if contact_name in self.personas:
            persona_config = self.personas[contact_name]
            if persona_config.get("enabled", True):
                return persona_config.get("system_prompt", self.default_persona)

        # 模糊匹配（OCR可能识别错误）
        for key, persona_config in self.personas.items():
            if key == "default":
                continue
            if (contact_name in key or key in contact_name) and persona_config.get("enabled", True):
                logging.info(f"大脑：使用模糊匹配人设 '{key}' 对应联系人 '{contact_name}'")
                return persona_config.get("system_prompt", self.default_persona)

        return self.default_persona

    def think_and_reply(self, new_messages_context: list, contact_name: str = None) -> str:
        """
        接收消息并生成回复，支持联系人专属人设
        :param new_messages_context: 消息上下文列表
        :param contact_name: 当前联系人名字（用于选择人设）
        :return: 生成的回复文本
        """
        if not new_messages_context:
            return ""

        # 根据联系人选择对应的 system_prompt
        persona = self._get_persona_for_contact(contact_name)
        messages_prompt = [{"role": "system", "content": persona}]

        # ... 其余逻辑保持不变 ...
```

**Engine 调用修改** (`core/engine.py:156`):

```python
# 在调用 think_and_reply 时传递当前联系人信息
reply_text = self.brain.think_and_reply(new_msgs, current_contact)
```

#### 6.2.3 后端API扩展

在 `main.py` 的 `AppApi` 类中添加人设管理接口：

```python
# 人设管理相关API
def get_contact_personas(self):
    """获取所有联系人人设配置"""
    # 实现逻辑...

def add_contact_persona(self, contact_name, persona_data):
    """添加新的联系人人设"""
    # 实现逻辑...

def update_contact_persona(self, contact_name, persona_data):
    """更新现有联系人人设"""
    # 实现逻辑...

def delete_contact_persona(self, contact_name):
    """删除联系人人设"""
    # 实现逻辑...

def set_default_persona(self, persona_data):
    """设置默认人设"""
    # 实现逻辑...

def get_persona_templates(self):
    """获取人设模板库"""
    # 实现逻辑...
```

#### 6.2.4 前端UI设计

**人设管理页面**（参考现有的模型管理UI）：

```
┌─────────────────────────────────────────┐
│  联系人人设管理          [+ 添加人设]    │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐ │
│  │ [张三] 工作伙伴       [启用中]      │ │
│  │     专业、简洁、礼貌的商务语气        │ │
│  │              [✓] [编辑] [删除]      │ │
│  └─────────────────────────────────────┘ │
│  ┌─────────────────────────────────────┐ │
│  │ [李四] 好朋友         [启用中]      │ │
│  │     轻松、幽默、可以开玩笑           │ │
│  │     [✓] [编辑] [删除]              │ │
│  └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│  默认人设设置                            │
│  ┌─────────────────────────────────────┐ │
│  │ 适用于所有未配置特定人设的联系人      │ │
│  │                    [编辑默认人设]    │ │
│  └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

**人设编辑弹窗**：

```
┌─────────────────────────────────────────┐
│  编辑人设 - 张三                         │
├─────────────────────────────────────────┤
│  联系人名称: [张三                      ] │
│  人设名称:   [工作伙伴                  ] │
│  人设描述: [专业、简洁、礼貌的商务语气  ] │
│                                         │
│  快速模板:                              │
│  [商务专业] [轻松随意] [温暖关怀]       │
│                                         │
│  System Prompt:                         │
│  ┌─────────────────────────────────────┐ │
│  │ 你正在回复重要的工作伙伴，请注意：   │ │
│  │ 1. 语气要专业、简洁、礼貌...        │ │
│  │ 2. 回复要直接切中要点...            │ │
│  │ （支持多行文本编辑）                 │ │
│  └─────────────────────────────────────┘ │
│                                         │
│  状态: ⦿ 启用  ○ 禁用                   │
│                                         │
│           [取消]      [保存]            │
└─────────────────────────────────────────┘
```

### 6.3 开发实施计划

| 阶段 | 任务 | 交付物 | 预估工时 | 难度 |
|------|------|--------|----------|------|
| **阶段1** | **核心逻辑实现** | | | |
| 1.1 | 扩展配置文件结构，添加人设配置段 | `config.yaml` 更新 | 30分钟 | 低 |
| 1.2 | 修改 AgentBrain 类支持动态人设 | `agent.py` 修改 | 1.5小时 | 中 |
| 1.3 | 修改 Engine 传递联系人信息 | `engine.py` 修改 | 30分钟 | 低 |
| 1.4 | 手动配置测试基础功能 | 测试验证 | 1小时 | 低 |
| **阶段2** | **后端API开发** | | | |
| 2.1 | 实现人设CRUD后端接口 | `main.py` API扩展 | 2小时 | 中 |
| 2.2 | 添加配置验证和错误处理 | 验证逻辑 | 1小时 | 中 |
| 2.3 | API接口测试 | 测试用例 | 1小时 | 低 |
| **阶段3** | **前端UI开发** | | | |
| 3.1 | 设计人设列表页面UI | HTML/CSS | 1.5小时 | 中 |
| 3.2 | 实现人设编辑弹窗 | HTML/CSS/JS | 2小时 | 中 |
| 3.3 | 集成后端API调用 | JavaScript | 1.5小时 | 中 |
| 3.4 | 添加人设模板功能 | 全栈 | 1小时 | 低 |
| **阶段4** | **测试与优化** | | | |
| 4.1 | 功能完整性测试 | 测试报告 | 1小时 | 低 |
| 4.2 | 模糊匹配算法优化 | 算法改进 | 1小时 | 中 |
| 4.3 | 性能优化和错误处理 | 代码优化 | 1小时 | 中 |
| 4.4 | UI/UX 优化 | 体验改进 | 1小时 | 低 |

**总工时估算：17-20小时**

### 6.4 技术难点与解决方案

#### 难点1：联系人名字OCR识别偏差
**问题**：OCR可能将"张三"识别成"张三"或"张 三"，导致精确匹配失败

**解决方案**：
```python
def _fuzzy_match_contact(self, recognized_name: str) -> str:
    """模糊匹配联系人名字"""
    # 1. 去除空格和特殊字符
    clean_name = re.sub(r'[\s\-_]', '', recognized_name)

    # 2. 精确匹配
    if clean_name in self.personas:
        return clean_name

    # 3. 包含关系匹配
    for key in self.personas.keys():
        clean_key = re.sub(r'[\s\-_]', '', key)
        if clean_name in clean_key or clean_key in clean_name:
            return key

    # 4. 编辑距离匹配（最后手段）
    best_match = None
    min_distance = float('inf')
    for key in self.personas.keys():
        distance = levenshtein_distance(clean_name, key)
        if distance < min_distance and distance <= 2:  # 最多允许2个字符差异
            min_distance = distance
            best_match = key

    return best_match
```

#### 难点2：人设配置的实时生效
**问题**：用户在UI中修改人设后，已运行的Engine如何立即生效

**解决方案**：
- 在 `AgentBrain.__init__()` 中使用文件监听（`watchdog`库）
- 当检测到配置文件变化时，自动重新加载人设配置
- 添加热重载API接口，支持手动触发配置刷新

#### 难点3：人设Prompt的版本管理
**问题**：用户可能希望保存多个人设版本，方便A/B测试

**解决方案**：
- 在配置中支持 `versions` 字段
- UI中提供版本历史查看和回滚功能
- 支持导出/导入单个人设配置

### 6.5 测试验证方案

#### 功能测试
- [ ] 默认人设正常工作
- [ ] 精确匹配联系人人设
- [ ] 模糊匹配OCR错误的联系人名
- [ ] 人设启用/禁用功能
- [ ] 人设CRUD操作
- [ ] 配置持久化和加载

#### 兼容性测试
- [ ] 向后兼容（无人设配置时使用默认）
- [ ] 配置文件格式变更处理
- [ ] API错误处理和降级

#### 性能测试
- [ ] 人设切换响应时间
- [ ] 大量联系人配置下的查询性能
- [ ] 配置文件大小对启动时间的影响

#### 用户体验测试
- [ ] UI操作流程直观性
- [ ] 人设编辑器易用性
- [ ] 模板功能实用性
- [ ] 错误提示友好性

### 6.6 后续扩展方向

#### V1.2 高级功能
- **分组管理**：将联系人分组，统一设置人设
- **上下文记忆**：为不同联系人保存对话历史
- **时间窗口人设**：不同时间段使用不同人设
- **条件触发人设**：根据消息内容动态选择人设

#### V1.3 智能化
- **AI辅助配置**：让大模型根据描述生成人设Prompt
- **效果分析**：统计不同人设的回复效果
- **自动优化**：根据反馈自动调整人设参数
- **人市场**：用户之间分享和交易优质人设

### 6.7 风险评估与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| OCR识别偏差导致匹配失败 | 高 | 中 | 实现多重模糊匹配算法 |
| 配置文件过于复杂 | 中 | 低 | 提供模板和预设，简化配置 |
| 性能影响（大量联系人） | 低 | 低 | 使用字典索引，保证O(1)查询 |
| 用户配置不当导致回复异常 | 中 | 中 | 添加配置验证和预览功能 |
| 向后兼容性问题 | 低 | 高 | 严格保持默认行为不变 |

### 6.8 成功标准

✅ **功能完整性**：
- 用户可以通过UI为任意联系人配置专属人设
- 支持人设的增删改查操作
- 默认人设作为兜底方案

✅ **用户体验**：
- UI操作直观简洁
- 人设模板库降低配置门槛
- 实时预览回复效果

✅ **技术质量**：
- 代码结构清晰，易于维护
- 充分的错误处理和日志记录
- 性能影响控制在可接受范围内

✅ **兼容性**：
- 完全向后兼容V1.0
- 现有用户无需任何改动即可升级
- 配置文件格式支持平滑迁移

---

**开发优先级：高**
**预计完成周期：2-3周（兼职开发）**
**技术债务：无（架构设计完善，可安全实施）**

---

## 七、 V1.2 版本详细规划

该版本主要聚焦于高级人设管理、上下文记忆增强以及视觉底层能力的优化。V1.2 版本的核心 Slogan 为：**“轻量化伴随 与 工业级安全”**。

### 7.0 V1.2 开发实施路径（工程指引）

### 7.1 核心高级功能规划（V1.2 重点特性）

**联系人上下文记忆 (Contextual Memory)**
- **目标**：彻底解决当前单轮对话的局限，为不同的联系人独立保存对话历史记录。
- **价值**：大模型可以通过回顾前文，实现更加连贯、符合逻辑的多轮对话交互，避免“鱼的记忆”。
- **机制补充（重要欠缺**）：**上下文截断与记忆摘要 (Context Summarization)**。由于大模型有上下文 Token 限制，不能无限量传入历史记录。必须引入滑动窗口机制（仅保留最近 N 轮），或使用向量数据库（RAG）提取长线记忆，亦或在对话过长时触发一次本地模型“总结历史”，从而在不过拟合硬件和 API 费用的基础上实现“长久记忆”。
- **隐私保护**：针对历史对话信息存留，必须加入本地加密存储或混淆机制，保障个人聊天记录的安全性。

**动态与条件触发人设 (Dynamic Persona Routing)**
- **时间窗口人设**：支持根据一天中的不同时间段自动应用不同的人设（例如，工作时间使用“商务专业”人设，下班后切换为“轻松随意”或“勿扰模式”人设）。
- **Prompt 内聚判定 (替代原系统级外部路由)**：将“根据对方情绪动态调整语气”的逻辑直接写入 System Prompt，如“如果对方语调愤怒则安抚，正常询问则专业回复”，依靠大模型单次推理直接出结果，避免引发冗余的 API 延迟与成本翻倍。

**分组与批量管理 (Group Management)**
- **目标**：在现有的 Aliases（别名）机制之上，进一步引入正式的分组管理功能。
- **机制**：允许将联系人按关系建立群组（如“VIP客户”、“亲戚”、“球友”），并针对整个分组统一设置人设，进一步降低多联系人的配置成本。

**单次手动重试 (Regenerate) 机制 (替代过度工程的 A/B 测试)**
- **目标**：在非标准化的日常聊天中，极难量化“回复好坏”的指标，做自动化数据回收属于过度工程。
- **降级方案**：在紧凑版 UI 上增加一个“换种说法”的快捷按钮，赋予用户在关键时刻对 AI 回复进行快速干预与重生的控制权。

### 7.2 底层系统与体验优化（针对当前局限性）

**基于特征的相对锚点定位 (Anchor Positioning) (替代原比例计算模式)**
- **避坑修正**：由于微信桌面端采用流式布局（Flex），缩放窗口时内部元素**不按比例**缩放，单纯数学乘除计算会导致严重错位。
- **优化方向**：利用 OpenCV 锁定“发送按钮”或“表情图标”，以此为锚点推算输入框的相对像素偏移量，从根本上解决分辨率或者窗口拉伸导致坐标系失效的问题。

**深色模式（Dark Mode）兼容性探索**
- **优化方向**：当前 MVP 阶段强制依赖微信浅色模式下的硬编码像素颜色（如气泡和红点颜色）。V1.2 将着手探索降低对特定颜色的绝对依赖，尝试引入更具鲁棒性的特征匹配算法，以期逐步兼容微信的深色模式。

**OCR 模糊匹配与排错升级**
- **优化方向**：持续改进因 OCR 识别误差（如将“张三”识别为带空格的“张 三”）导致人设匹配失败或逻辑判定错误的问题。通过优化模糊匹配算法，进一步提升纯视觉提取文本的容错率。

**多模态异常处理扩展**
- **优化方向**：目前系统遇到语音、图片、视频或小程序时，只能触发固定的降级兜底话术（如“设备听不了语音”）。未来规划中可以探索与图像识别（视觉大模型）或基础音频解析能力的对接，或者使兜底话术更加自然、随机，从而提升拟人化体验。

### 7.3 UI 界面全面重构（悬浮/紧凑模式）

**背景与痛点**：此前的预研与设计偏向于宽屏 UI（类似桌面管理后台的尺寸），这导致在较小的屏幕或单显示器上使用时，一旦打开本应用就会大面积遮挡原有的微信界面，甚至让用户无法在此过程中同时查看微信内容和调整 AI 参数。

为解决此冲突，V1.2 将全面重构应用界面，转向**“伴随式、紧凑型”交互理念**：

**1. 紧凑型主面板 (Compact / Sidebar View)**
- **设计形态**：尺寸类似微信手机版的宽度或是桌面小工具（Widget），可吸附停靠在屏幕边缘或微信窗体旁（宽幅建议不超过 `350px-400px`）。
- **核心信息展示**：在紧凑空间内，**只保留最核心的运行态信息**。
  - 当前激活联系人名称、正在使用的人设方案。
  - AI 引擎当前状态信号灯（🟢 监听中 / 🟡 OCR读取思考中 / 🔴 当前暂停接管）。
  - 最近一条交互的精简日志。

**2. 灵活的视图切换机制 (Toggle / Drawer Interface)**
- **分层抽屉操作**：像配置所有人设、分组管理、以及深入看完整日志等“占地面积大”的低频操作，隐藏在侧边抽屉或作为二级弹窗。平时只需保持极致小巧的主面板进行实时监控。

**3. 全局急停按钮及状态可视化 (Kill-Switch & Visibility)**
- **硬件级视觉表现**：在 UI 最显眼的位置配备一个「全局暂停/恢复接管」的急停开关（Panic Button）。由于多轮记忆会导致 AI 自主性更强，当用户发现 AI “即将说出不合适的话”时，能第一时间在主悬浮窗一键阻断程序的拟人键鼠行为。

**4. 始终置顶与透明度控制 (Always-on-top & Opacity)**
- 允许界面开启“始终置顶”，这样即使用户在和微信其他联系人交互，也能够余光瞥见 AI 助手的状态；并辅以界面透明度调节，进一步减少对底层窗口内容的视线遮挡。

### 7.4 自动化底线安全防御 (核心护城河)

本章节是 V1.2 真正实现“轻量化伴随 与 工业级安全”的关键门槛。如果未来要将本套系统挂载到商业场景中，以下是在自动执行层面对抗失控风险的最后防线：

**1. 物理级防冲突中断 (Human Override)**
- **致命隐患**：AI 抢夺鼠标准备粘贴时，用户恰好在用电脑工作，导致剪贴板内容错乱发送给错误的人。
- **规划要求**：引入底层的**键鼠活动监听**。在执行“移、点、粘、发”短暂步骤期间，一旦侦测到任何物理键鼠位移或敲击，立刻触发 Abort（阻断本次执行）。

**2. 状态机与防重播机制**
- **致命隐患**：程序崩溃重启、或网络波动卡顿时，可能会重新扫描到之前的红点，将上一句话再发送一次（重复鞭尸）。
- **规划要求**：在记忆库中增加 `Last_Replied_Hash`（最后回复指纹）。执行视觉操作前，先对比目标对象的文本哈希，拦截重复触发。

**3. 输出风控护栏 (Guardrails)**
- **致命隐患**：大模型产生幻觉，被对方的话术套路，直接答应转账或承诺退款。
- **规划要求**：在 AI 输出文本与“写入剪贴板”之间，强行插入一层**本地正则黑名单**。精准拦截“转账、密码、身份证、同意退款”等高危词，命中则挂起并闪烁 UI 报警，转交人工干预。

**4. 基于备注名的身份锚定**
- **致命隐患**：仅依靠 OCR 提取“微信昵称”来关联上下文记忆库，一旦对方修改网名，该联系人的记忆将彻底断代。
- **规划要求**：强制引入系统认领机制，要求必须为重点联系人设置微信“**备注名**”。同时在 UI 的分组管理中，补充一个“**记忆库合并 (Merge)**”功能，用于抢救昵称变更导致的断链。

### 开发路径计划

为了保证落地质量，并将 `ARCHITECTURE.md` 与 `README_STRUCTURE.md` 中前后端解耦的理念贯彻到底，V1.2 版本的开发必须严格遵循以下五个阶段进行：

| 阶段 | 代号/模块 | 涉及核心文件 | 核心开发任务与目标 | 风险与避坑提示 |
| :--- | :--- | :--- | :--- | :--- |
| **P2** | **工业级安全防线** <br>*(防御底座)* | `core/action.py`<br>`core/engine.py`<br>`core/agent.py` | 1. **失焦与抢夺焦点防御**：放弃极其容易误杀的 pynput 监听。改为在 `action.py` 进行 `Ctrl+V` 注入和回车前，使用 `win32gui.GetForegroundWindow()` 检查微信是否仍为绝对焦点；并通过记录执行前后的鼠标位置，若发生大幅跳跃，说明用户在抢鼠标，立即放弃发送引发 Abort。<br>2. **防鞭尸机**：在 `engine.py` 每次回复后，将哈希特征写入本地缓存 `Last_Replied_Hash`，下次扫描强制比对。<br>3. **正则风控阻断**：在 `agent.py` 的 LLM 返回后强插拦截网，拦截“退款、密码”等极其危险的话术。 | 强烈不建议使用 `pynput` 全局监听键盘鼠标，因为 AI 自己的 `pyautogui` 动作也会被捕获引发自己杀自己的 Bug；采用 `win32gui` 获取绝对窗口句柄检查即可防篡位。 |
| **P3** | **记忆流与上下文摘要引擎** <br>*(长线多轮大脑)* | `core/agent.py`<br>`data/` 目录 | 1. 构建轻量化的本地数据库（如 `sqlite3` 或独立 `json`），按**微信备注名**分区存储历史记录。<br>2. 实现**滑动窗口机制**与**Token 截断摘要**方案（超出阈值时清理旧历史或调用小模型总结）。<br>3. 前端 UI 提供手动“**记忆网合并 (Merge)**” 功能，以应对对方改名带来的断链灾难。 | 别把记忆直接耦合进配置或主流程里。应该新开一个独立的 `core/memory_manager.py` 来处理历史数据的读写和加解密防护。 |
| **P4** | **视觉锚点定位进化** <br>*(跨屏鲁棒性)* | `core/vision.py` | 1. 弃用固定绝对坐标系或简单的比例数学缩放。<br>2. 在 `vision.py` 中增加 OpenCV `cv2.matchTemplate` 的“特征找图”机制（如寻找输入框旁的特定图标），通过算出图案特征的中心点，将其设为（0,0）原点逆推输入点击位置。 | 不要找大面积特征（容易变），找类似聊天输入框右上角那种像素极其稳定的局部图标作为锚点。 |
| **P5** | **Prompt 内聚与人机接力** <br>*(业务层闭环)* | `core/agent.py`<br>`ui/index.html` | 1. 更新 `system_prompt`，在一大堆设定中直接写入条件触发判定指令（“若对方抱怨则安抚...”）。<br>2. 部署前端的「单次手动重试」功能，点击后删除最后一条本地回复记录，让 LLM 拿原提问重新生成一次。 | |

---
